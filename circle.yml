

machine:
  xcode:
    version: 8.3.2

  environment:
    XCODE_SCHEME: CircleCI-Sample
    XCODE_WORKSPACE: CircleCI-Sample.xcworkspace
    GYM_CODE_SIGNING_IDENTITY: 'iPhone Developer: Vadim Kastritsky (23V8GT7RMV)'
    APPNAME: CircleCI-Sample
    PROFILE_UUID: 1e31620a-a485-4f0e-b343-471ea6f1767e
dependencies:
  override:
    - pod install:
        timeout: 1300
deployment:
  testflight:
    branch: master
    commands:
      - ./scripts/add-key.sh
      - ./scripts/testflight.sh
      - ./scripts/remove-key.sh
